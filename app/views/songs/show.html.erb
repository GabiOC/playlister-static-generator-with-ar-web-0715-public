<!DOCTYPE html>
<html>
  <head>
    <title><%= @song.name %> </title>
  </head>
  <body>
    <h1><%= @song.name %> </h1>
    <h2>Artist: <%= @song.artist.name %> </h2>
    <h2>Genre: <%= @song.genre.name %></h2>
  </body>
</html>
